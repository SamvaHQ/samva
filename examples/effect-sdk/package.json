{
  "name": "effect-sdk-example",
  "version": "0.0.0",
  "private": true,
  "description": "Send email with Samva's Effect-native SDK entrypoint.",
  "type": "module",
  "scripts": {
    "build": "bun build src/send.ts src/server.ts --outdir dist --target bun",
    "send": "bun run src/send.ts",
    "serve": "bun run src/server.ts",
    "smoke": "bun run src/smoke.ts",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "effect": "4.0.0-rc.112",
    "samva": "^0.5.0"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0"
  }
}
